diff --git a/dist/api/openai-codex-responses.js b/dist/api/openai-codex-responses.js
index c9fe5d1..0efbf3c 100644
--- a/dist/api/openai-codex-responses.js
+++ b/dist/api/openai-codex-responses.js
@@ -475,6 +475,7 @@ async function processStream(response, output, stream, model, grammarToolInputPr
         grammarToolInputProperties,
         resolveServiceTier: resolveCodexServiceTier,
         applyServiceTierPricing: (usage, serviceTier) => applyServiceTierPricing(usage, serviceTier, model),
+        onOutputItemDone: options?.onOutputItemDone,
     });
 }
 class CodexApiError extends Error {
@@ -1182,6 +1183,7 @@ async function processWebSocketStream(url, body, headers, output, stream, model,
             grammarToolInputProperties,
             resolveServiceTier: resolveCodexServiceTier,
             applyServiceTierPricing: (usage, serviceTier) => applyServiceTierPricing(usage, serviceTier, model),
+            onOutputItemDone: options?.onOutputItemDone,
         });
         if (options?.signal?.aborted) {
             keepConnection = false;
diff --git a/dist/api/openai-responses-shared.d.ts b/dist/api/openai-responses-shared.d.ts
index 2d49e73..6767f6e 100644
--- a/dist/api/openai-responses-shared.d.ts
+++ b/dist/api/openai-responses-shared.d.ts
@@ -6,6 +6,7 @@ export interface OpenAIResponsesStreamOptions {
     grammarToolInputProperties?: ReadonlyMap<string, string>;
     resolveServiceTier?: (responseServiceTier: ResponseCreateParamsStreaming["service_tier"] | undefined, requestServiceTier: ResponseCreateParamsStreaming["service_tier"] | undefined) => ResponseCreateParamsStreaming["service_tier"] | undefined;
     applyServiceTierPricing?: (usage: Usage, serviceTier: ResponseCreateParamsStreaming["service_tier"] | undefined) => void;
+    onOutputItemDone?: (item: unknown, model: Model<Api>) => void | Promise<void>;
 }
 export interface ConvertResponsesMessagesOptions {
     includeSystemPrompt?: boolean;
diff --git a/dist/api/openai-responses-shared.js b/dist/api/openai-responses-shared.js
index c35771d..4dcfab7 100644
--- a/dist/api/openai-responses-shared.js
+++ b/dist/api/openai-responses-shared.js
@@ -573,6 +573,7 @@ export async function processResponsesStream(openaiStream, output, stream, model
         }
         else if (event.type === "response.output_item.done") {
             const item = event.item;
+            await options?.onOutputItemDone?.(item, model);
             applyMessagePhaseStopReason(item);
             const slot = getOrCreateSlot(event.output_index, item);
             if (item.type === "reasoning" && slot?.type === "thinking") {
diff --git a/dist/api/openai-responses.js b/dist/api/openai-responses.js
index 1023156..b533b98 100644
--- a/dist/api/openai-responses.js
+++ b/dist/api/openai-responses.js
@@ -118,6 +118,7 @@ export const stream = (model, context, options) => {
                 serviceTier: options?.serviceTier,
                 grammarToolInputProperties,
                 applyServiceTierPricing: (usage, serviceTier) => applyServiceTierPricing(usage, serviceTier, model),
+                onOutputItemDone: options?.onOutputItemDone,
             });
             if (options?.signal?.aborted) {
                 throw new Error("Request was aborted");
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 3b6ec74..89ef7ee 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -1,5 +1,6 @@
 export type { Static, TSchema } from "typebox";
 export { Type } from "typebox";
+export declare const responseOutputItemHookVersion = 1;
 export type { AnthropicEffort, AnthropicOptions, AnthropicThinkingDisplay } from "./api/anthropic-messages.ts";
 export type { AzureOpenAIResponsesOptions } from "./api/azure-openai-responses.ts";
 export type { BedrockOptions, BedrockThinkingDisplay } from "./api/bedrock-converse-stream.ts";
diff --git a/dist/index.js b/dist/index.js
index 2f1880e..a8c0552 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1,4 +1,5 @@
 export { Type } from "typebox";
+export const responseOutputItemHookVersion = 1;
 export * from "./api/lazy.js";
 export * from "./auth/context.js";
 export * from "./auth/credential-store.js";
diff --git a/dist/types.d.ts b/dist/types.d.ts
index 77b46da..9f1f838 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -68,6 +68,8 @@ export interface ProviderRequestOptions<TModel = Model<Api>> {
      * Return undefined to keep the payload unchanged.
      */
     onPayload?: (payload: unknown, model: TModel) => unknown | undefined | Promise<unknown | undefined>;
+    /** Optional callback for completed provider-native Responses output items. */
+    onOutputItemDone?: (item: unknown, model: TModel) => void | Promise<void>;
     /**
      * Optional callback invoked after an HTTP response is received.
      */
