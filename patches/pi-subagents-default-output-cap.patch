diff --git a/src/intercom/result-intercom.ts b/src/intercom/result-intercom.ts
index 8af08eb..2d98757 100644
--- a/src/intercom/result-intercom.ts
+++ b/src/intercom/result-intercom.ts
@@ -13,6 +13,8 @@ import {
 	type SubagentRunMode,
 	SUBAGENT_RESULT_INTERCOM_DELIVERY_EVENT,
 	SUBAGENT_RESULT_INTERCOM_EVENT,
+	DEFAULT_MAX_OUTPUT,
+	truncateOutput,
 } from "../shared/types.ts";
 
 export function resolveSubagentResultStatus(input: {
@@ -277,7 +279,7 @@ export function buildSubagentResultIntercomPayload(input: GroupedResultIntercomM
 		...(firstChild?.sessionPath ? { sessionPath: firstChild.sessionPath } : {}),
 		message: "",
 	};
-	payload.message = formatSubagentResultIntercomMessage(payload);
+	payload.message = truncateOutput(formatSubagentResultIntercomMessage(payload), DEFAULT_MAX_OUTPUT).text;
 	return payload;
 }
 
diff --git a/src/runs/background/notify.ts b/src/runs/background/notify.ts
index c7a8282..c3a46c8 100644
--- a/src/runs/background/notify.ts
+++ b/src/runs/background/notify.ts
@@ -14,7 +14,7 @@ import {
 	createCompletionBatcher,
 	resolveCompletionBatchConfig,
 } from "./completion-batcher.ts";
-import { SUBAGENT_ASYNC_COMPLETE_EVENT, SUBAGENT_FOREGROUND_COMPLETE_EVENT, type ParallelHandoffReference, type SubagentState } from "../../shared/types.ts";
+import { DEFAULT_MAX_OUTPUT, SUBAGENT_ASYNC_COMPLETE_EVENT, SUBAGENT_FOREGROUND_COMPLETE_EVENT, truncateOutput, type ParallelHandoffReference, type SubagentState } from "../../shared/types.ts";
 
 export interface SubagentNotifyDetails {
 	agent: string;
@@ -151,7 +151,8 @@ interface PendingCompletion {
 function sendCompletion(pi: Pick<ExtensionAPI, "sendMessage">, items: PendingCompletion[]): boolean {
 	if (items.length === 0) return true;
 	const details = items.map((item) => item.details);
-	const content = details.length === 1 ? formatSingleCompletion(details[0]!) : formatGroupedCompletion(details);
+	const formatted = details.length === 1 ? formatSingleCompletion(details[0]!) : formatGroupedCompletion(details);
+	const content = truncateOutput(formatted, DEFAULT_MAX_OUTPUT).text;
 	try {
 		pi.sendMessage(
 			{
diff --git a/src/runs/background/subagent-runner.ts b/src/runs/background/subagent-runner.ts
index 8c5eb55..6baf434 100644
--- a/src/runs/background/subagent-runner.ts
+++ b/src/runs/background/subagent-runner.ts
@@ -3688,14 +3688,15 @@ async function runSubagent(
 	let summary = results.map((r) => `${r.agent}:\n${r.output}`).join("\n\n");
 	let truncated = false;
 
-	if (maxOutput) {
-		const config = { ...DEFAULT_MAX_OUTPUT, ...maxOutput };
-		const lastArtifactPath = results[results.length - 1]?.artifactPaths?.outputPath;
-		const truncResult = truncateOutput(summary, config, lastArtifactPath);
-		if (truncResult.truncated) {
-			summary = truncResult.text;
-			truncated = true;
-		}
+	const outputConfig = { ...DEFAULT_MAX_OUTPUT, ...maxOutput };
+	const outputArtifactPath = (result: StepResult): string | undefined => {
+		const candidate = result.artifactPaths?.outputPath;
+		return candidate && fs.existsSync(candidate) ? candidate : undefined;
+	};
+	const truncResult = truncateOutput(summary, outputConfig);
+	if (truncResult.truncated) {
+		summary = truncResult.text;
+		truncated = true;
 	}
 
 	const resultMode = config.resultMode ?? statusPayload.mode;
@@ -3846,7 +3847,7 @@ async function runSubagent(
 			results: results.map((r) => ({
 				agent: r.agent,
 				context: r.context,
-				output: r.output,
+				output: truncateOutput(r.output, outputConfig, outputArtifactPath(r)).text,
 				error: r.error,
 				protocolError: r.protocolError,
 				success: r.success,
diff --git a/src/runs/foreground/execution.ts b/src/runs/foreground/execution.ts
index 176c077..f254ab0 100644
--- a/src/runs/foreground/execution.ts
+++ b/src/runs/foreground/execution.ts
@@ -1450,16 +1450,13 @@ export async function runSync(
 				metadataPath: options.artifactConfig?.includeMetadata === false ? undefined : artifactPathsResult.metadataPath,
 			}));
 		}
-		if (options.maxOutput) {
-			const config = { ...DEFAULT_MAX_OUTPUT, ...options.maxOutput };
-			const truncationResult = truncateOutput(result.finalOutput ?? "", config, artifactPathsResult.outputPath);
-			if (truncationResult.truncated) result.truncation = truncationResult;
-		}
-	} else if (options.maxOutput) {
-		const config = { ...DEFAULT_MAX_OUTPUT, ...options.maxOutput };
-		const truncationResult = truncateOutput(result.finalOutput ?? "", config);
-		if (truncationResult.truncated) result.truncation = truncationResult;
 	}
+	const outputConfig = { ...DEFAULT_MAX_OUTPUT, ...options.maxOutput };
+	const outputArtifactPath = artifactPathsResult && options.artifactConfig?.includeOutput !== false
+		? artifactPathsResult.outputPath
+		: undefined;
+	const truncationResult = truncateOutput(result.finalOutput ?? "", outputConfig, outputArtifactPath);
+	if (truncationResult.truncated) result.truncation = truncationResult;
 
 	if (options.sessionFile && (existsSync(options.sessionFile) || result.messages?.length)) {
 		result.sessionFile = options.sessionFile;
diff --git a/src/runs/foreground/subagent-executor.ts b/src/runs/foreground/subagent-executor.ts
index 14c7f7d..d8fe3f5 100644
--- a/src/runs/foreground/subagent-executor.ts
+++ b/src/runs/foreground/subagent-executor.ts
@@ -115,6 +115,8 @@ import {
 	SUBAGENT_CONTROL_EVENT,
 	SUBAGENT_CONTROL_INTERCOM_EVENT,
 	SUBAGENT_FOREGROUND_COMPLETE_EVENT,
+	DEFAULT_MAX_OUTPUT,
+	truncateOutput,
 	checkSubagentDepth,
 	resolveTopLevelParallelConcurrency,
 	resolveTopLevelParallelMaxTasks,
@@ -1371,13 +1373,34 @@ async function resumeAsyncRun(input: {
 }
 
 function resultSummaryForIntercom(result: SingleResult): string {
-	const output = getSingleResultOutput(result);
+	const output = result.truncation?.text ?? getSingleResultOutput(result);
 	if (result.exitCode !== 0 && result.error) {
 		return output ? `${result.error}\n\nOutput:\n${output}` : result.error;
 	}
 	return output || result.error || "(no output)";
 }
 
+function boundForegroundModelOutput(
+	result: AgentToolResult<Details>,
+	maxOutput?: MaxOutputConfig,
+): AgentToolResult<Details> {
+	const textItems = result.content.filter((item): item is Extract<typeof item, { type: "text" }> => item.type === "text");
+	const text = textItems.map((item) => item.text).join("\n");
+	const candidateArtifactPath = result.details?.results.length === 1
+		? result.details.results[0]?.artifactPaths?.outputPath
+		: undefined;
+	const artifactPath = candidateArtifactPath && fs.existsSync(candidateArtifactPath) ? candidateArtifactPath : undefined;
+	const truncation = truncateOutput(text, { ...DEFAULT_MAX_OUTPUT, ...maxOutput }, artifactPath);
+	if (!truncation.truncated) return result;
+	return {
+		...result,
+		content: [
+			{ type: "text", text: truncation.text },
+			...result.content.filter((item) => item.type !== "text"),
+		],
+	};
+}
+
 function formatFailedSingleRunOutput(result: SingleResult, displayOutput: string): string {
 	const error = result.error || "Failed";
 	const output = displayOutput.trim();
@@ -4090,22 +4113,43 @@ export function createSubagentExecutor(deps: ExecutorDeps): {
 				nestedForegroundStarted = true;
 			}
 			if (hasChain && effectiveParams.chain) {
-				const result = await runChainPath(execData, deps);
+				const result = boundForegroundModelOutput(
+					withResolvedContext(
+						withForkThinkingNotes(await runChainPath(execData, deps), forkThinkingDowngrades),
+						contextPolicy.contextSummary,
+					),
+					effectiveParams.maxOutput,
+				);
 				writeNestedForegroundEvent("subagent.nested.completed", result);
-				return withResolvedContext(withForkThinkingNotes(result, forkThinkingDowngrades), contextPolicy.contextSummary);
+				return result;
 			}
 			if (hasTasks && effectiveParams.tasks) {
-				const result = await runParallelPath(execData, deps);
+				const result = boundForegroundModelOutput(
+					withResolvedContext(
+						withForkThinkingNotes(await runParallelPath(execData, deps), forkThinkingDowngrades),
+						contextPolicy.contextSummary,
+					),
+					effectiveParams.maxOutput,
+				);
 				writeNestedForegroundEvent("subagent.nested.completed", result);
-				return withResolvedContext(withForkThinkingNotes(result, forkThinkingDowngrades), contextPolicy.contextSummary);
+				return result;
 			}
 			if (hasSingle) {
-				const result = await runSinglePath(execData, deps);
+				const result = boundForegroundModelOutput(
+					withResolvedContext(
+						withForkThinkingNotes(await runSinglePath(execData, deps), forkThinkingDowngrades),
+						contextPolicy.contextSummary,
+					),
+					effectiveParams.maxOutput,
+				);
 				writeNestedForegroundEvent("subagent.nested.completed", result);
-				return withResolvedContext(withForkThinkingNotes(result, forkThinkingDowngrades), contextPolicy.contextSummary);
+				return result;
 			}
 		} catch (error) {
-			const errorResult = withForkThinkingNotes(toExecutionErrorResult(effectiveParams, error, contextPolicy.contextSummary), forkThinkingDowngrades);
+			const errorResult = boundForegroundModelOutput(
+				withForkThinkingNotes(toExecutionErrorResult(effectiveParams, error, contextPolicy.contextSummary), forkThinkingDowngrades),
+				effectiveParams.maxOutput,
+			);
 			if (nestedForegroundStarted) writeNestedForegroundEvent("subagent.nested.completed", errorResult);
 			return errorResult;
 		} finally {
diff --git a/src/shared/types.ts b/src/shared/types.ts
index 79e1d38..12f612c 100644
--- a/src/shared/types.ts
+++ b/src/shared/types.ts
@@ -1590,6 +1590,19 @@ function formatBytes(bytes: number): string {
 	return `${(bytes / (1024 * 1024)).toFixed(1)}MB`;
 }
 
+function utf8Prefix(value: string, maxBytes: number): string {
+	if (maxBytes <= 0) return "";
+	const bytes = Buffer.from(value, "utf-8");
+	if (bytes.length <= maxBytes) return value;
+	let end = maxBytes;
+	let start = end - 1;
+	while (start > 0 && (bytes[start]! & 0xc0) === 0x80) start--;
+	const lead = bytes[start]!;
+	const sequenceLength = lead < 0x80 ? 1 : lead < 0xe0 ? 2 : lead < 0xf0 ? 3 : 4;
+	if (start + sequenceLength > end) end = start;
+	return bytes.subarray(0, end).toString("utf-8");
+}
+
 export function truncateOutput(
 	output: string,
 	config: Required<MaxOutputConfig>,
@@ -1597,36 +1610,26 @@ export function truncateOutput(
 ): TruncationResult {
 	const lines = output.split("\n");
 	const bytes = Buffer.byteLength(output, "utf-8");
+	const maxBytes = Math.max(0, Math.floor(config.bytes));
+	const maxLines = Math.max(0, Math.floor(config.lines));
 
-	if (bytes <= config.bytes && lines.length <= config.lines) {
+	if (bytes <= maxBytes && lines.length <= maxLines) {
 		return { text: output, truncated: false };
 	}
 
-	let truncatedLines = lines;
-	if (lines.length > config.lines) {
-		truncatedLines = lines.slice(0, config.lines);
-	}
-
-	let result = truncatedLines.join("\n");
-	if (Buffer.byteLength(result, "utf-8") > config.bytes) {
-		let low = 0;
-		let high = result.length;
-		while (low < high) {
-			const mid = Math.floor((low + high + 1) / 2);
-			if (Buffer.byteLength(result.slice(0, mid), "utf-8") <= config.bytes) {
-				low = mid;
-			} else {
-				high = mid - 1;
-			}
-		}
-		result = result.slice(0, low);
-	}
-
-	const keptLines = result.split("\n").length;
-	const marker = `[TRUNCATED: showing first ${keptLines} of ${lines.length} lines, ${formatBytes(Buffer.byteLength(result))} of ${formatBytes(bytes)}${artifactPath ? ` - full output at ${artifactPath}` : ""}]\n`;
+	const artifact = artifactPath ? ` - full output at ${artifactPath.replace(/\s+/g, " ")}` : "";
+	const marker = `[TRUNCATED: bounded prefix of ${lines.length} lines, ${formatBytes(bytes)} total${artifact}]`;
+	const boundedMarker = maxLines > 0 ? utf8Prefix(marker, maxBytes) : "";
+	const bodyLineBudget = Math.max(0, maxLines - 1);
+	const separator = boundedMarker && bodyLineBudget > 0 ? "\n" : "";
+	const bodyByteBudget = Math.max(0, maxBytes - Buffer.byteLength(boundedMarker) - Buffer.byteLength(separator));
+	const body = bodyLineBudget > 0 && bodyByteBudget > 0
+		? utf8Prefix(lines.slice(0, bodyLineBudget).join("\n"), bodyByteBudget)
+		: "";
+	const text = `${boundedMarker}${body ? separator + body : ""}`;
 
 	return {
-		text: marker + result,
+		text,
 		truncated: true,
 		originalBytes: bytes,
 		originalLines: lines.length,
